// Page script — runs when the page loads.
// Use the 'flow' SDK to fetch data and update state.
// Example:
// const incidents = await flow.query('incidents', { status: 'open' })
// flow.setState({ openCount: incidents.length })
